---
title: Unsupported columns
description: Blueprint column types and methods not yet supported by the migration parser.
---

import { Content as UnsupportedColumns } from '../../../generated/unsupported-columns.md';

:::note
This list is generated from `BlueprintUnsupportedInterface`, so it always matches the installed version.
:::

<UnsupportedColumns />

When a migration contains an unsupported column type, the command prints a warning and adds a `@todo` comment in the data transfer object, projection and projector. See [Migrations](/laravel-event-sourcing-generator/guide/migrations/#limitations-unsupported-column-types) for a worked example.
